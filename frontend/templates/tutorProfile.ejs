<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UTD Tutors</title>

    <link href="../styles/style.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light px-3">
        <a href="../index.html">
            <img src="../assets/logos/IQnicLogo.png" alt="Logo" class="mx-2" style="max-width: 70px;">
        </a>
        <div class="container-fluid d-flex justify-content-between">
            <form class="d-flex search-container">
                <input class="form-control me-2 search-input" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-dark" type="button" onclick="searchFunction()">Search</button>
            </form>
            <a class="btn btn-dark" href="./pages/personalProfile.html">Profile</a>
        </div>
    </nav>

    <div class="row mt-4">
        <div class="tutor-profile-main">
            <div class="tutor-profile row j">
                <div class="text-center">
                    <img src="../assets/profile/tutorprofile.jpg" class="rounded-circle mb-4" alt="Profile Picture" style="max-height: 70px">
                    <h1 class="mb-3"><%= tutorRenderData.name %></h1>
                    <p class="lead"> <%= tutorRenderData.aboutMe %> </p>

                    <p class="lead mx-2">
                        <% tutorRenderData.subjects.forEach(item => { %> 
                            <%= item %>
                        <% }) %>
                    </p>

                    <h3 class="mt-5">Available Times</h3>
                    <% tutorRenderData.times.forEach(item => { %>
                        <div class="row d-flex justify-content-center m-3">
                            <p class="lead" style="max-width: fit-content;"><%= item %></p>
                            <button class="btn btn-success" style="max-width: fit-content;" onclick="handleAptSignup(`<%= item %>`)">Sign up</button>
                        </div>
                    <% }) %>
                    <button class="favorites-btn btn btn-dark" onclick="handleFavorites(this)">Add to Favorites</button>
                </div>
            </div>
        </div>
    </div>

    <footer class="absolute-bottom p-4 mt-2 text-center">
        UTD Senior Design Project 2023 --- Team 19 
    </footer>

    <script src="../scripts/tutorProfile.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

</body>
</html>